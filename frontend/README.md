# TaskRank — frontend

Готовый интерфейс платформы задач бизнеса и студенческих команд: создание карточки, уточнение деталей, прозрачный рейтинг готовности, публикация, предложения команд и подтверждение результатов. Интерфейс на русском языке.

Стек: React 18, TypeScript, Vite, React Router, Tailwind CSS и lucide-react. Компоненты интерфейса собственные. Состояние хранится в React; `localStorage` и `sessionStorage` не используются.

## Установка и запуск

Требуются **Node.js 22 или новее** и npm. Все команды ниже выполняются из папки `frontend/`:

```sh
npm ci
```

Создайте `.env` по примеру `.env.example`, если собственного файла настроек ещё нет:

```sh
cp .env.example .env
```

В PowerShell эквивалентная команда — `Copy-Item .env.example .env`. Существующие настройки сохраняйте.

```sh
npm run dev
```

Откройте [http://localhost:5173](http://localhost:5173). Порт фиксирован: при занятом `5173` Vite завершится с ошибкой, а не выберет другой порт. Backend уже разрешает origins `http://localhost:5173` и `http://127.0.0.1:5173`. Для доступа с другого origin требуется настройка CORS на backend.

Backend запускается отдельно по [инструкции backend](../backend/README.md). Она описывает создание Python-окружения, установку зависимостей, настройки и заполнение демонстрационных данных через `python -m app.seed`. API должен отвечать на [http://127.0.0.1:8000/api/v1](http://127.0.0.1:8000/api/v1), Swagger — на [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs). `127.0.0.1` означает компьютер, на котором открыт браузер.

## Окружение

Актуальный пример хранится в `.env.example`:

```dotenv
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
VITE_USE_MOCKS=false
```

| Переменная | Назначение |
| --- | --- |
| `VITE_API_BASE_URL` | Полный префикс API, включая `/api/v1`. Значение по умолчанию: `http://127.0.0.1:8000/api/v1`. |
| `VITE_USE_MOCKS` | Только строка `true` включает локальный демонстрационный API. По умолчанию используется настоящий backend. |
| `VITE_API_URL` | Совместимость со старой настройкой: базовый адрес сервера без `/api/v1`. Используется только при отсутствии `VITE_API_BASE_URL`; префикс добавляется автоматически. |

После изменения `.env` перезапустите Vite. Значения с префиксом `VITE_` входят в клиентскую сборку; секреты в них не помещаются.

### Демонстрация без backend

Укажите в `.env`:

```dotenv
VITE_USE_MOCKS=true
```

Перезапустите frontend. Мок-режим предоставляет бизнес-пользователя, команды, задачи разных уровней готовности и предложения. Данные находятся **только в памяти вкладки** и возвращаются к исходному состоянию после перезагрузки. Они не отправляются в настоящий API и не переносятся между режимами.

В реальном режиме задачи, профили, предложения и результаты сохраняются backend и доступны после обновления страницы. Выбранный демо-пользователь хранится в памяти интерфейса: после обновления его может потребоваться выбрать заново.

## Страницы и сценарий

В шапке переключается демо-пользователь: бизнес или представитель команды. Список пользователей загружается через `GET /demo/actors`; защищённые запросы содержат `X-Actor-ID`. При смене пользователя данные кабинета запрашиваются заново.

| Маршрут | Что доступно |
| --- | --- |
| `/catalog` | Каталог опубликованных задач: фильтры по теме и готовности, сортировка по рейтингу сервера и страницы результатов. |
| `/tasks/:id` | Публичная карточка и форма отклика команды: идея, план, срок, необязательная HTTP/HTTPS-ссылка. |
| `/business/tasks` | Собственные черновики и публикации, количество откликов и переход к редактированию. |
| `/new` | Создание задачи из свободного текста и темы; три кликабельных примера слабого описания. |
| `/new/questions?task=ID` | Вопросы помощника, ответы, предлагаемая карточка и явное применение сведений. |
| `/tasks/:id/edit` | Редактирование всех десяти полей и темы, расшифровка рейтинга, подтверждение и публикация. |
| `/tasks/:id/proposals` | Сравнение предложений, ручное принятие или отказ, подтверждение этапов с описанием результата. |
| `/team/profile` | Название команды, интересы, навыки и технологии. |
| `/team/proposals` | Отправленные предложения и решения бизнеса. |
| `/team/progress` | Баллы XP, уровень команды и достижения по имеющимся данным. |
| `/recommendations` | Задачи с рейтингом от 40 и совпадениями по тегам профиля команды. |

Сквозная демонстрация:

1. Выберите бизнес, откройте `/new` и используйте один из примеров или собственный текст.
2. Получите минимум три вопроса. Ответьте на известные вопросы; остальные можно пропустить.
3. Просмотрите предлагаемую карточку и нажмите «Применить и открыть карточку».
4. Дополните поля. Панель отдельно показывает подтверждённый рейтинг и возможный результат после подтверждения. Подсказки «до +N баллов» переводят к нужному полю.
5. Нажмите «Подтвердить карточку» после проверки сведений. Рейтинг обновится; достижение нового уровня сопровождается уведомлением.
6. Опубликуйте задачу. Она появится в каталоге; низкий рейтинг не мешает публикации или откликам.
7. Переключитесь на команду, при необходимости заполните профиль и отправьте предложение из публичной карточки.
8. Вернитесь в бизнес-кабинет и примите или отклоните предложение вручную. Можно принять несколько команд; автоматического выбора нет.
9. У принятого предложения подтвердите этап и опишите проверенный результат. Команда получает XP: **прототип — 20**, **пилот — 30**, **передача результата — 50**. Повтор идентичного подтверждения не удваивает баллы.
10. Переключитесь на команду и откройте «Мой прогресс». В реальном режиме обновление страницы сохраняет серверные результаты.

Геймификация отражает существующие данные. Уровень команды выводится из `team.points`; достижения — из профиля, предложений, решений и баллов. Самостоятельного клиентского начисления XP нет. Рекомендации вычисляются во frontend по совпадениям тегов во всём доступном каталоге: это понятный фильтр интересов, не AI-оценка команд и не назначение исполнителя.

## Контракт и сохранение

Источник фактического контракта — [backend/docs/api.md](../backend/docs/api.md); дополнительные правила интеграции — [frontend-handoff.md](../backend/docs/frontend-handoff.md).

- JSON использует `snake_case`. Типы расположены в `src/types.ts`, запросы — в `src/api/client.ts`.
- `POST /tasks/:id/assist` возвращает предложение без сохранения. Оно применяется только по действию пользователя с учётом `based_on_revision`.
- `PUT /tasks/:id/card` заменяет карточку целиком. Frontend передаёт все поля и актуальную `expected_revision`, а затем использует новую `revision` из ответа.
- Подтверждение относится к текущей редакции карточки целиком. Backend не предоставляет отдельного подтверждения каждого поля. `/confirm` вызывается только после явного нажатия «Подтвердить карточку».
- Публикация — отдельное действие после подтверждения. Каталог показывает сохранённый опубликованный снимок. Последующие правки приватной карточки не меняют этот снимок до следующей публикации.
- `rating.score`, `rating.preview_score`, уровни и расшифровка приходят с backend. Frontend анимирует число и отображает подсказки, но не рассчитывает и не отправляет рейтинг.
- При `409 STALE_REVISION` введённый текст сохраняется на экране. Пользователь загружает новую серверную версию, сравнивает её со своей и явно выбирает, какую сохранить. Автоматической перезаписи правок нет.
- Во время сохранения формы блокируются от повторной отправки и изменения сохраняемых полей. Ошибки отображаются по-русски; доступны повторные действия.

## Ограничения демо

Авторизация демонстрационная: `X-Actor-ID` выбирает пользователя. Это не полноценная регистрация или защищённая сессия. Реальные ограничения доступа проверяет backend, но публичный запуск потребует настоящей авторизации; `DEMO_MODE=false` на backend блокирует защищённые операции до её подключения.

При `provider=stub` экран явно сообщает, что вопросы сформированы локальной заглушкой. `fallback_reason`, если он присутствует, показывается пользователю. Работающая LLM не предполагается автоматически: для Ollama нужна отдельная настройка backend по его README. Помощник не публикует карточку и не подтверждает сведения за человека.

Рейтинг отражает полноту подтверждённого описания, а не доказанную достоверность или качество задачи. В приложении нет загрузки файлов, встроенного чата, платежей или автоматического выбора команды. Ссылка на прототип открывает внешний ресурс, указанный командой.

## Проверки и сборка

Из `frontend/`:

```sh
npm run typecheck
npm run test
npm run build
```

`typecheck` проверяет TypeScript. `test` запускает Vitest; текущие три теста в `src/api/mocks.test.ts` проверяют ручное подтверждение, публикацию снимка, устаревшие редакции, доступ к слабым задачам, решения по предложениям, идемпотентное начисление этапа и ограничения доступа в мок-API. Эти тесты не заменяют проверку реального backend и браузерного сценария.

При интеграционной проверке сборка `npm run build` и три теста Vitest были успешно выполнены. После дальнейших изменений повторите команды выше и сквозной сценарий через интерфейс.

Для просмотра собранной версии:

```sh
npm run preview
```

Адрес — [http://localhost:4173](http://localhost:4173). Если собранный frontend должен обращаться к backend с этого адреса, origin `http://localhost:4173` необходимо разрешить в CORS backend. Для стандартной локальной демонстрации используйте `npm run dev` на порту 5173.

### Текущий экземпляр для QA

Во время разработки frontend и backend запущены локально на портах 5173 и 8000. Для проверки использована изолированная SQLite-база `frontend/.runtime/taskrank-demo.db`: рабочая база команды не подменялась. Папка `.runtime/` содержит временное окружение и результаты запуска, исключена из Git и не входит в исходные файлы для передачи. Процессы разработки могут завершиться после закрытия окружения; стандартный запуск описан выше.

## Файлы для передачи

Ниже — полный список исходных файлов, настроек, lock-файла и документации `frontend/` на момент подготовки. `node_modules/`, `dist/`, `.runtime/`, локальные `.env` и `*.tsbuildinfo` не входят в передачу исходников.

```text
.env.example
.gitignore
README.md
index.html
package-lock.json
package.json
postcss.config.js
src/App.tsx
src/api/client.ts
src/api/errors.ts
src/api/mocks.test.ts
src/api/mocks.ts
src/components/Layout.tsx
src/components/ScorePanel.tsx
src/components/TaskCard.tsx
src/components/ui.tsx
src/context/RoleContext.tsx
src/context/ToastContext.tsx
src/hooks/useAsync.ts
src/main.tsx
src/pages/BusinessTasksPage.tsx
src/pages/CatalogPage.tsx
src/pages/EditorPage.tsx
src/pages/NewTaskPage.tsx
src/pages/ProposalsPage.tsx
src/pages/QuestionsPage.tsx
src/pages/TaskDetailPage.tsx
src/pages/TeamPages.tsx
src/styles.css
src/types.ts
src/ui/fields.ts
src/ui/levels.ts
src/vite-env.d.ts
tailwind.config.js
tsconfig.json
vite.config.ts
```

Обновить список можно из `frontend/`:

```sh
rg --files --hidden -g '!node_modules/**' -g '!dist/**' -g '!.runtime/**' -g '!*.tsbuildinfo' -g '!.env'
```

Изменения ограничены папкой `frontend/`. Общий README, контракт, backend и данные команды не входят в изменения фронтенда. Коммиты, push и слияние выполняются отдельно владельцем репозитория.
